# distro <%= node[:hadoop][:cloudera_distro_name] %> version <%= node[:hadoop][:cdh_version] %>
<%- distro = node[:hadoop][:cloudera_distro_name] || node[:lsb][:codename] %>
<%- repo   = "#{distro}-#{node[:hadoop][:cdh_version]}" %>
deb http://archive.cloudera.com/debian <%= repo %> contrib
deb-src http://archive.cloudera.com/debian <%= repo %> contrib

# bump 0.9.7
